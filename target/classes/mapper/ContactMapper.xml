<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC
    "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zhongruan.bizcard.dao.ContactDao">
  <select id="findAll" resultType="com.zhongruan.bizcard.entity.ContactEntity">
    select sid,sname,age
    from student
    order by sid
  </select>
   <select id="findById" resultType="com.zhongruan.bizcard.entity.ContactEntity">
    select sid,sname,age
    from student
    where sid = #{sid}
  </select>
 <select id="yanzheng" resultType="java.util.Map">
 select sid, count(*) as counter
    from student
    where sid = #{sid}
 </select>
    <select id="login" resultType="java.util.Map">
    select sid, count(*) as counter
    from student
    where sid = #{sid}
      and age = #{age}
  </select>
  
  <select id="findByName" resultType="com.zhongruan.bizcard.entity.ContactEntity">
    select sid,sname,age
    from student
    where locate(#{keyword}, sname) > 0
  </select>
  
  <insert id="add"  >
  insert into student(sid, sname, age) values (#{sid},#{sname},#{age})
  </insert>
  <delete id="shan">
  	delete from  student  where sid=#{sid}
  </delete>
</mapper>  

